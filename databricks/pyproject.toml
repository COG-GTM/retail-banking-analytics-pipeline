[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retail_banking"
version = "1.0.0"
description = "Retail banking analytics pipeline - Databricks Delta Lake port"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
