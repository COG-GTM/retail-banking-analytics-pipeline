# Runtime + test dependencies for the PySpark retail-banking analytics pipeline.
# Pinned for reproducibility. PySpark 3.5.x supports Java 11/17.
pyspark==3.5.1
delta-spark==3.2.0
numpy==1.26.4
# distutils was removed from the stdlib in Python 3.12; pyspark.ml still imports
# it, so setuptools must provide the shim.
setuptools>=69
pytest==8.2.2
pytest-cov==5.0.0
