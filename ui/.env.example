# Base URL of the Spring Boot data-products API (used by the dev proxy)
VITE_API_BASE_URL=http://localhost:8080

# Set to "true" to serve data from CSV-derived JSON fixtures instead of the API
VITE_USE_MOCKS=true

# Displayed in the header status bar
VITE_OPERATOR=OP-4417
VITE_ENVIRONMENT=DEV
