# Runtime dependencies for snowflake_credentials.py (MBA-2203 / TICKET-02).
# Installed on the Synapse Spark pool and on engineer workstations.
azure-identity>=1.16.0
azure-keyvault-secrets>=4.8.0
cryptography>=42.0.0
snowflake-connector-python>=3.7.0
