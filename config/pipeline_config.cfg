# =============================================================================
# Pipeline Configuration - Retail Banking Customer Analytics
# =============================================================================
# Environment: DEV | UAT | PROD
# Last Updated: 2026-04-10
#
# This file is DATA, not shell. It is parsed (never sourced) by
# config/load_config.sh: one KEY=VALUE per line, values are literal
# (no variable expansion or command substitution) and are validated
# before export. Derived paths (BTEQ_DIR, LOG_DIR, SAS_BATCH, ...) and
# run timestamps are computed by the loader.
#
# The file must be owned by the ETL service account or root and must not
# be group/world writable (e.g. root:etl 0640); the loader refuses to run
# otherwise.
# =============================================================================

# -- Teradata Connection -------------------------------------------------------
TD_SERVER=tdprod.corp.bankdemo.com
TD_USERNAME=svc_etl_pipeline
TD_LOGMECH=LDAP

# -- Database References -------------------------------------------------------
DB_CORE=CORE_BANKING_DB          # Operational customer/account data
DB_TXN=TXN_PROCESSING_DB         # Transaction processing tables
DB_STG=ETL_STAGING_DB            # BTEQ staging/intermediate tables
DB_DP=DATA_PRODUCTS_DB           # Final certified data products

# -- SAS Configuration ---------------------------------------------------------
SAS_HOME=/opt/SASHome/SASFoundation/9.4
SAS_CONFIG=/opt/SASHome/SASConfig/Lev1
SAS_AUTOEXEC=/opt/SASHome/SASConfig/Lev1/autoexec.sas

# -- Path Configuration --------------------------------------------------------
PIPELINE_HOME=/opt/etl/retail_banking_analytics

# -- Run Parameters ------------------------------------------------------------
LOOKBACK_MONTHS=12
RISK_SCORE_THRESHOLD=700

# -- Logging -------------------------------------------------------------------
LOG_LEVEL=INFO   # DEBUG | INFO | WARN | ERROR
