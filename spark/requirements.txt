# Python dependencies for the PySpark analytics layer (spark/).
# Spark 3.5.x runs on Java 8/11/17.
pyspark>=3.5.1,<3.6
numpy>=1.24
pandas>=2.0
# setuptools provides the distutils shim PySpark 3.5 imports on Python 3.12+.
setuptools>=68
# Test-only
pytest>=8.0
